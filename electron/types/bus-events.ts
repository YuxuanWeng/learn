export enum BusEventEnum {
  /** 窗口恢复 */
  RecoveryDialog = 'bus-recovery-dialog',
  /** 恢复完成后，关闭用于限制完整退出的flag */
  RecoveryCancelFlag = 'bus-recovery-dialog-cancel-flag',
  /** 恢复首页 */
  LoadLayoutWindow = 'bus-load-layout-window'
}
